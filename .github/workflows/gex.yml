name: Build GEX each night (Full Gamma Suite v7.0)

on:
  schedule:
    - cron: '0 1 * * *'   # Run daily at 1:00 AM UTC
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pandas numpy matplotlib requests

      - name: Run GEX Builder (Full Suite)
        env:
          MARKETDATA_KEY: ${{ secrets.MARKETDATA_KEY }}
        run: python gex_builder_v7.py

      - name: Commit Outputs
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add *.csv *.txt *.png || echo "No new files to add"
          git commit -m "Nightly GEX update (v7.0 Full Suite)" || echo "No changes to commit"
          git push || echo "Push skipped"
