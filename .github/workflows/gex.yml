name: Build GEX each night

on:
  schedule:
    - cron: "0 1 * * *"  # every night at 1 AM UTC
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run GEX builder
        env:
          MARKETDATA_KEY: ${{ secrets.MARKETDATA_KEY }}
        run: python gex_builder.py

      - name: Commit CSVs and latest.txt
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add *.csv latest.txt || echo "No files to add"
          git commit -m "Daily GEX update" || echo "No changes to commit"
          git push || echo "Push skipped"
