name: Build GEX Twice Daily (Full Gamma Suite)

on:
  schedule:
    # Runs at 1:00 AM UTC (Nightly) and 1:00 PM UTC (Pre-Market)
    - cron: '0 1,13 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: Install dependencies
        run: |
          pip install -r requirements.txt || echo "No requirements.txt found"
          pip install pandas numpy matplotlib requests

      - name: Verify script exists
        run: |
          if [ ! -f "gex_builder.py" ]; then
            echo "❌ gex_builder.py not found in repo root!"
            exit 1
          fi

      - name: Run GEX Builder (Full Suite)
        env:
          MARKETDATA_KEY: ${{ secrets.MARKETDATA_KEY }}
        run: python gex_builder.py

      - name: Commit Outputs
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add *.csv *.txt *.png *.pine || echo "No new files to add"
          git commit -m "Twice-daily GEX update" || echo "No changes to commit"
          git push || echo "Push skipped"
